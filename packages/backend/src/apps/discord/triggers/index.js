// Define a named function that returns an empty array
// by default. This function can be customized later if needed.
const createEmptyArray = () => [];

// Export the function for use in other modules
module.exports = createEmptyArray;

