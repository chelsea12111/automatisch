// Array of scopes for API permissions
const scopes: string[] = [
  'account_info.read',
  'files.metadata.read',
  'files.content.write',
  'files.content.read',
];

export { scopes };


// Array of scopes for API permissions
const scopes = [
  'account_info.read',
  'files.metadata.read',
  'files.content.write',
  'files.content.read',
];

export { scopes };
