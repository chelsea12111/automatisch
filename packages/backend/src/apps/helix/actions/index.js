// Import the newChat module and its default export
import newChat from './new-chat/index.js';

// Create an array with the newChat module as its only element
const chatModules = [newChat];

// Export the chatModules array as the default export of this module
export default chatModules;

